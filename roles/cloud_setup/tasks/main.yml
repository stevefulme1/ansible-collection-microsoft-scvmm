---
- name: Validate required variables
  ansible.builtin.assert:
    that:
      - cloud_setup_name is defined and cloud_setup_name | length > 0
    fail_msg: "Required variable: cloud_setup_name"

- name: Configure SCVMM cloud
  block:
    - name: Create or update cloud
      stevefulme1.scvmm.scvmm_cloud:
        name: "{{ cloud_setup_name }}"
        description: "{{ cloud_setup_description }}"
        host_groups: "{{ cloud_setup_host_groups }}"
        state: "{{ cloud_setup_state }}"
        scvmm_host: "{{ cloud_setup_scvmm_host }}"
        scvmm_port: "{{ cloud_setup_scvmm_port }}"
      register: _cloud_setup_result

    - name: Configure cloud capacity
      stevefulme1.scvmm.scvmm_cloud:
        name: "{{ cloud_setup_name }}"
        capacity:
          cpu_count: "{{ cloud_setup_capacity_cpu_count }}"
          memory_mb: "{{ cloud_setup_capacity_memory_mb }}"
          storage_gb: "{{ cloud_setup_capacity_storage_gb }}"
          vm_count: "{{ cloud_setup_capacity_vm_count }}"
        scvmm_host: "{{ cloud_setup_scvmm_host }}"
        scvmm_port: "{{ cloud_setup_scvmm_port }}"
        state: present
      when: cloud_setup_capacity_enabled

    - name: Assign logical networks to cloud
      stevefulme1.scvmm.scvmm_cloud:
        name: "{{ cloud_setup_name }}"
        logical_networks: "{{ cloud_setup_logical_networks }}"
        scvmm_host: "{{ cloud_setup_scvmm_host }}"
        scvmm_port: "{{ cloud_setup_scvmm_port }}"
        state: present
      when: cloud_setup_logical_networks | length > 0

    - name: Assign library shares
      stevefulme1.scvmm.scvmm_cloud:
        name: "{{ cloud_setup_name }}"
        library_shares: "{{ cloud_setup_library_shares }}"
        scvmm_host: "{{ cloud_setup_scvmm_host }}"
        scvmm_port: "{{ cloud_setup_scvmm_port }}"
        state: present
      when: cloud_setup_library_shares | length > 0

    - name: Configure user roles
      stevefulme1.scvmm.scvmm_user_role:
        name: "{{ item.name }}"
        cloud: "{{ cloud_setup_name }}"
        members: "{{ item.members }}"
        permissions: "{{ item.permissions | default([]) }}"
        scvmm_host: "{{ cloud_setup_scvmm_host }}"
        scvmm_port: "{{ cloud_setup_scvmm_port }}"
        state: present
      loop: "{{ cloud_setup_user_roles }}"
      loop_control:
        label: "{{ item.name }}"
      when: cloud_setup_user_roles | length > 0

    - name: Display cloud status
      ansible.builtin.debug:
        msg: "Cloud '{{ cloud_setup_name }}' configured with {{ cloud_setup_host_groups | length }} host groups"

  rescue:
    - name: Report cloud setup failure
      ansible.builtin.fail:
        msg: >-
          Cloud setup failed for '{{ cloud_setup_name }}'.
          Error: {{ ansible_failed_result.msg | default('Unknown error') }}
