---
argument_specs:
  main:
    short_description: "Configure DPM backup integration"
    options:
      scvmm_backup_setup_host:
        description: Target host address
        type: str
        required: true
      scvmm_backup_setup_username:
        description: Authentication username
        type: str
      scvmm_backup_setup_password:
        description: Authentication password
        type: str
      scvmm_backup_setup_api_key:
        description: API key for authentication
        type: str
      scvmm_backup_setup_validate_certs:
        description: Validate SSL certificates
        type: bool
        default: true
      scvmm_backup_setup_state:
        description: Desired state
        type: str
        default: present
        choices: [present, absent]
