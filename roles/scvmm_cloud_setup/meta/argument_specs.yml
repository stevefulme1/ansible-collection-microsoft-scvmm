---
argument_specs:
  main:
    short_description: "Configure SCVMM clouds and host groups"
    options:
      scvmm_cloud_setup_host:
        description: Target host address
        type: str
        required: true
      scvmm_cloud_setup_username:
        description: Authentication username
        type: str
      scvmm_cloud_setup_password:
        description: Authentication password
        type: str
        no_log: true
      scvmm_cloud_setup_api_key:
        description: API key for authentication
        type: str
        no_log: true
      scvmm_cloud_setup_validate_certs:
        description: Validate SSL certificates
        type: bool
        default: true
      scvmm_cloud_setup_state:
        description: Desired state
        type: str
        default: present
        choices: [present, absent]
