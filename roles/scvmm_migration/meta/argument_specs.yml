---
argument_specs:
  main:
    short_description: "Live migrate VMs between hosts"
    options:
      scvmm_migration_host:
        description: Target host address
        type: str
        required: true
      scvmm_migration_username:
        description: Authentication username
        type: str
      scvmm_migration_password:
        description: Authentication password
        type: str
      scvmm_migration_api_key:
        description: API key for authentication
        type: str
      scvmm_migration_validate_certs:
        description: Validate SSL certificates
        type: bool
        default: true
      scvmm_migration_state:
        description: Desired state
        type: str
        default: present
        choices: [present, absent]
