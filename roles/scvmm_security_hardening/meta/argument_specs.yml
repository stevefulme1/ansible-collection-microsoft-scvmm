---
argument_specs:
  main:
    short_description: "Apply security policies and shielded VMs"
    options:
      scvmm_security_hardening_host:
        description: Target host address
        type: str
        required: true
      scvmm_security_hardening_username:
        description: Authentication username
        type: str
      scvmm_security_hardening_password:
        description: Authentication password
        type: str
      scvmm_security_hardening_api_key:
        description: API key for authentication
        type: str
      scvmm_security_hardening_validate_certs:
        description: Validate SSL certificates
        type: bool
        default: true
      scvmm_security_hardening_state:
        description: Desired state
        type: str
        default: present
        choices: [present, absent]
